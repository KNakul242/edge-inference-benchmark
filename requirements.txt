# Core — pinned for cross-environment reproducibility
# Platform: Mac M5 + Fedora Linux (CPU). See requirements-colab.txt for Colab T4.
torch==2.3.1
torchvision==0.18.1
numpy==1.26.4

# Model export and inference
ultralytics==8.2.103
onnx==1.16.2
onnxruntime==1.18.1          # Mac M5: includes CoreML EP. Linux: CPU EP only.
coremltools==7.2              # macOS only — install separately on Mac M5

# Dataset and evaluation
# faster-coco-eval is the primary evaluator — native numpy 2.x wheels, drop-in API.
# pycocotools is kept as a fallback for environments where it installs cleanly (numpy 1.x).
faster-coco-eval
pycocotools==2.0.7
opencv-python==4.10.0.84

# Config and environment
python-dotenv==1.0.1
PyYAML==6.0.2
psutil==7.0.0              # Process RSS measurement in memory_profiler (captures native allocations)

# Testing
pytest==8.3.5
pytest-cov==5.0.0
