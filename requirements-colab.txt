# Colab T4 GPU — TensorRT benchmarking only
# Document the Colab runtime version used at execution time (Cell 1 prints it).
torch==2.3.1+cu118
torchvision==0.18.1+cu118
numpy==1.26.4

ultralytics==8.2.103
onnx>=1.18.0,<1.22.0  # see notebooks/tensorrt_colab.ipynb Cell 1 comment — wheel availability, not exact-pinned
onnxruntime-gpu==1.18.1
# TensorRT: pinned exact, not left to "whatever Colab's latest is today" —
# 11.x removed the BuilderFlag/platform_has_fast precision API this project
# uses. See notebooks/tensorrt_colab.ipynb Cell 1 and docs/issue-log/
# 2026-09-11-colab-trt-version-pin.md.
tensorrt==10.16.1.11
pycuda==2024.1

pycocotools==2.0.7
opencv-python==4.10.0.84
python-dotenv==1.0.1
PyYAML==6.0.2

pytest==8.3.5
pytest-cov==5.0.0
