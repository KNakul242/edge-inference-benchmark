# Colab T4 GPU — TensorRT benchmarking only
# Document the Colab runtime version used at execution time (Cell 1 prints it).
torch==2.3.1+cu118
torchvision==0.18.1+cu118
numpy==1.26.4

ultralytics==8.2.103
onnx==1.16.2
onnxruntime-gpu==1.18.1
# TensorRT: install from Colab's pre-built wheel matching CUDA version
# tensorrt==8.6.x — verify against Colab CUDA at time of use
pycuda==2024.1

pycocotools==2.0.7
opencv-python==4.10.0.84
python-dotenv==1.0.1
PyYAML==6.0.2

pytest==8.3.5
pytest-cov==5.0.0
